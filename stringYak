public String stringYak(String str) {

	/*
	Suppose the string "yak" is unlucky.
	Given a string, return a version where
	all the "yak" are removed, but the
	"a" can be any char. The "yak" strings
	will not overlap.

	stringYak("yakpak") → "pak"
	stringYak("pakyak") → "pak"
	stringYak("yak123ya") → "123ya"
	*/

	String newString = "";
	String finalString = "";

	for (int i = 0; i < str.length(); i += 3) {

		int endIndex = i + 3;

		if (endIndex > str.length()) endIndex = str.length();

		newString = str.substring(i, endIndex);

		if (newString.equals("yak")) {
			newString = "";
		} else {
			newString = str.substring(i, endIndex);
		}

		finalString += newString;

	}

	return finalString;

}
