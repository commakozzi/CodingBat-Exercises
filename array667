public int array667(int[] nums) {

  /*
  Given an array of ints, return the number
  of times that two 6's are next to each other
  in the array. Also count instances where
  the second "6" is actually a 7.

  array667({6, 6, 2}) → 1
  array667({6, 6, 2, 6}) → 1
  array667({6, 7, 2, 6}) → 1
  */

  int count = 0;

  for (int i = 0; i < nums.length - 1; i++) {
    int x = nums[i];
    int y = nums[i + 1];
    if (x == 6 && y == 6 || x == 6 && y == 7) count++;

  }

  return count;

}
