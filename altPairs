public String altPairs(String str) {

  /*
  Given a string, return a string made of
  the chars at indexes 0,1, 4,5, 8,9 ...
  so "kittens" yields "kien".
  */

  String newString = "";
  String nextPair = "";

  for (int i = 0; i < str.length() - 2; i = i + 4) {
    nextPair = str.substring(i, i + 2);
    newString = newString + nextPair;
  }

  return newString;

}
