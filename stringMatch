public int stringMatch(String a, String b) {

	/*
	Given 2 strings, a and b, return the number
	of the positions where they contain the same
	length 2 substring. So "xxcaazz" and "xxbaaz"
	yields 3, since the "xx", "aa", and "az"
	substrings appear in the same place
	in both strings.
	*/

	int count = 0;
	String newStringA = "";
	String newStringB = "";
	String shortest = "";

	// Find shortest
	if (a.length() < b.length()) {
		shortest = a;
	} else {
		shortest = b;
	}

	for (int i = 0; i < shortest.length() - 1; i++) {
		newStringA = a.substring(i, i + 2);
		newStringB = b.substring(i, i + 2);
		if (newStringA.equals(newStringB)) {
			count++;
		}
	}

	return count;

}
